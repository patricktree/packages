---
'@patricktree/commons-ecma': patch
---

Fix the typo in the error serialization helper by adding `serializeErrorWithCause` and keeping `serializeErrorWithClause` as a deprecated alias for backwards compatibility.
